#compdef config

_arguments -A '-*' '-l[configure local dotfiles]:local:->local'

local directory

case "$state" in
  local)
    directory="dotfiles-local"
    ;;
  "")
    directory="dotfiles"
    ;;
esac

_files -W "$HOME/$directory" -F "(*.md LICENSE bin)"
