# easily configure something in a dotfile
config() {
  local directory

  if [ "$1" = "-l" ]; then
    directory='dotfiles-local'
    shift
  else
    directory='dotfiles'
  fi

  vi "$HOME/$directory/$1"
}
