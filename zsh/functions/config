# easily configure something in a dotfile
config() {
  local directory

  if [ "$1" = "-l" ]; then
    directory='dotfiles-local'
    shift
  else
    directory='dotfiles'
  fi

  if [ -n "$1" ]; then
    (cd "$HOME/$directory" && vi "$HOME/$directory/$1")
  else
    cd "$HOME/$directory"
  fi
}
